package main

const CSS_CODE = `
  GtkEntry {
    background-image: none;
    border: none;
    box-shadow: none;
    font-size: 14pt;
  }
  GtkTreeView {
    background-color: transparent;
    font-size: 8pt;
  }
  GtkTreeView:selected {
    background-color: #59b2ed;
  }
  GtkTreeView.cell {
    padding: 6px;
  }
`
